import React from 'react';
import { connect } from 'react-redux';

const Cart = () => {
    return (
        <div>
            Add Cart Here
        </div>
    )
}

export default connect((state) => state)(Cart);

